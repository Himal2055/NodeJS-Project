<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>My Blog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }

        header {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px;
        }

        h1 {
            font-size: 36px;
        }

        nav {
            background-color: #555;
            color: #fff;
            padding: 10px;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav li {
            display: inline;
            margin-right: 20px;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .post {
            margin-bottom: 30px;
        }

        .post h2 {
            font-size: 24px;
            margin-top: 0;
        }

        .post p {
            line-height: 1.6;
        }

        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 10px;
        }
    </style>
</head>
<body>
    <header>
        <h1>CMS</h1>
    </header>

    <div class="container">
        <div class="post">
            <div class="author-name"><%- blog[0].title %></div>
            <div class="author-name"><%- blog[0].subTitle %></div>
        </div>

        <div class="post-content">
            <p><%- blog[0].description %></p>
        </div>

    <div class="post-footer">
        <!-- here toISOString() is to remove unecessary time zone data and slice cut and throw the data outside the parameter -->
        Published on <%- blog[0].createdAt.toISOString().slice(0,10) %> 
    </div>
    <button><a href="/delete/<%- blog[0].id %>">Delete</a></button>
    </div>

</body>
</html>
